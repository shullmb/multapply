const express = require('express');
const router = express.Router();

// GET /listings
router.get('/', (req, res) => {
  
})
// POST /listings

// GET /listings/:id

// PUT /listings/:id

// DELETE /listings/:id


module.exports = router;